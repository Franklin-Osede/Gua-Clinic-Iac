# 🔧 Configuración de Desarrollo del Widget GUA
# 
# INSTRUCCIONES:
# 1. Copia este archivo: cp .env.development.example .env.development
# 2. Edita .env.development con tus valores reales
# 3. El archivo .env.development NO se sube a git (está en .gitignore)

# URL del API Gateway (producción)
# Cambia esta URL si usas un entorno diferente (dev, pre, prod)
VITE_GUA_SERVICE_URL=https://ybymfv93yg.execute-api.eu-north-1.amazonaws.com/prod

# Clave AES para encriptación (solo desarrollo)
# Esta clave se usa para encriptar datos sensibles en desarrollo
# En producción, la clave viene del endpoint /bootstrap
VITE_AES_KEY=dGVzdC1hZXMta2V5LWZvci1kZXZlbG9wbWVudA==
