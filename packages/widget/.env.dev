# Configuración del Widget GUA Clinic
# Copia este archivo a .env.development y ajusta los valores si es necesario
# 
# IMPORTANTE: Este archivo NO se sube a git (está en .gitignore)
# Cada desarrollador debe crear su propio .env.development

# URL del API Gateway (SIEMPRE HTTPS en producción)
# Esta es la URL pública que usa el load balancer con certificado TLS
VITE_GUA_SERVICE_URL=https://ybymfv93yg.execute-api.eu-north-1.amazonaws.com/prod

# Clave AES para desarrollo (usar la misma que en producción)
VITE_AES_KEY=dGVzdC1hZXMta2V5LWZvci1kZXZlbG9wbWVudA==

# NOTA: Nunca uses la IP directa (http://13.53.43.4:3000) porque:
# 1. No tiene certificado HTTPS → causa "Mixed Content" en WordPress
# 2. No pasa por el load balancer → pierdes rate limiting y logs
# 3. No es estable → puede cambiar si reinicias ECS
